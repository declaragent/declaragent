---
id: index
title: Quickstart
slug: /quickstart
sidebar_label: Overview
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Quickstart — 10-minute path

From an empty laptop to a running local agent in under ten minutes — plus the
generated artifacts to take it to **GCP Cloud Run** (the container build/push and
`gcloud` steps are yours to run and add a few more minutes).

The short version:

```bash
# 1. Install
curl -sSL https://get.declaragent.dev | sh

# 2. Scaffold + run locally
declaragent init
declaragent up

# 3. Generate deploy artifacts (Dockerfile + service.yaml + printed gcloud steps)
declaragent deploy gcp-cloud-run
```

Every step has an escape hatch for non-Linux / offline / non-GCP environments — see the linked pages below.

## Walkthrough

1. **[Install](/quickstart/installing)** — pick one of curl, npm, or Homebrew.
2. **[Your first agent](/quickstart/first-agent)** — template-driven path. Run `declaragent init`, pick `concierge`, talk to the agent locally.
3. **[Conversational tour](/quickstart/conversational-tour)** 🎯 — the recommended path. Converse with Declaragent to build a two-agent fleet end to end in ~15 minutes.
4. **[Deploy to Cloud Run](/cookbook/deploy-cloud-run)** — takes the scaffolded fleet to a running Cloud Run service.

:::tip Which path should I take?
Use **Conversational tour** if you're new and want to see the headline capability first. Use **First agent** if you prefer templates + hand-editing YAML. Both converge on the same runtime — pick what matches your learning style.
:::

## What you'll end up with

```
my-agent/
├── agent.yaml               # identity + system prompt
├── .env.example             # credentials template
├── README.md                # the template's own runbook
├── channels.yaml            # ships with channel-using templates (incl. concierge)
├── event-sources.yaml       # optional — only for webhook/Kafka/etc templates
└── skills/
    └── concierge.md
```

Everything here is plain text. Commit it, branch it, `git revert` it — that's the entire point.

## Screenshots

_Screenshot: `declaragent init` walkthrough — placeholder, landing 2026-Q2._


## Cross-links

- Full [template catalog](/cookbook) in the Cookbook.
- If anything goes wrong, start with [Troubleshooting → install failed](/troubleshooting).
