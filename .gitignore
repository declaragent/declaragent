node_modules
dist
build
coverage
.DS_Store
*.log
.env
.env.*
!.env.example
.turbo
.cache
*.tsbuildinfo
# Docusaurus cache (docs-site/)
.docusaurus
# Credentials — defensive, should never land in the repo
.npmrc
# Runtime state created by `declaragent fleet run` + the daemon
.declaragent/
# macOS noise
.DS_Store
# Internal planning docs — kept local, not shipped (repo-root only;
# `docs-site/docs/` is the public Docusaurus surface)
/docs/
