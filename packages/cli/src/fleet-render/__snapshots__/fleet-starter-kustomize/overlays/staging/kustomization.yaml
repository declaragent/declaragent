apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fleet-starter-staging
resources:
  - ../../base
commonLabels:
  declaragent.io/environment: staging
patches:
  - target:
      kind: Deployment
      name: concierge
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: concierge
      spec:
        replicas: 2
        template:
          spec:
            containers:
              - name: declaragent
                resources:
                  limits:
                    cpu: 1000m
                    memory: 1Gi
  - target:
      kind: Deployment
      name: pr-reviewer
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: pr-reviewer
      spec:
        replicas: 2
        template:
          spec:
            containers:
              - name: declaragent
                resources:
                  limits:
                    cpu: 1000m
                    memory: 1Gi
commonAnnotations:
  declaragent.io/fleet: fleet-starter
  declaragent.io/environment: staging
