ANTHROPIC_API_KEY=sk-ant-...

# Optional. Leave unset to share an in-memory broker with rpc-client
# in the same process (local dev / CI). Set to your Kafka broker list
# to run across processes.
KAFKA_BROKERS=
